# v0.8 launch — capture checklist (human-gated)

These artifacts need a clean macOS environment + live installs and **cannot be
generated by an agent**. Each is gated on **Phases 23/24/25 confirmed prod-live**
(MCP server published + n8n/Anthropic rulesets live + v1.1 evidence-pack export
live). Capture during the launch window (2026-08-01/02), drop the files into the
sibling `screenshots/`, `screencaps/`, and `artifacts/` directories, and check
the boxes.

## Screenshots → `screenshots/`

- [ ] **Cursor Directory listing** — the `@hookwarden/mcp` entry as it appears in
      the Cursor MCP directory. *(Gate: MCP server live + directory-listed.)*
- [ ] **Continue Hub listing** — the `@hookwarden/mcp` entry in the Continue hub.
      *(Gate: MCP server live + hub-listed.)*

## Screencaps → `screencaps/`

- [ ] **SC#1 — Claude Desktop verifying a pack** — a screencap of Claude Desktop
      calling `verify_audit_chain` on a v1.1 evidence pack and returning
      `{ valid: true, … }`, then `{ valid: false, broken_at_row: N }` on a
      tampered pack. *(Gate: MCP server + v1.1 export live.)*

## Artifacts → `artifacts/`

- [ ] **SC#4 — `lsof` zero-network proof** — terminal capture showing
      `npx hookwarden scan` opens no outbound network connections (`lsof` /
      `sudo lsof -i` during a scan, no remote sockets). The "zero traffic to a
      third party" claim, demonstrated. *(Gate: v0.8 CLI published.)*
- [ ] **Signed sample evidence pack** — the exact `sample-pack.json` the
      `hookwarden.dev/eu-ai-act` page serves, copied here for press reuse, with
      its `verify-evidence-pack.mjs` PASS output. *(Available now — sourced from
      `packages/web/public/sample-pack.json` in the private repo.)*

## Window-gated reminder

The posting itself (HN / dev.to / X) and the production go-live of
`hookwarden.dev/eu-ai-act` are tracked in the private repo's
`26-LAUNCH-DAY-CHECKLIST.md` — not here. This file covers only the visual capture
artifacts that live in the public press kit.

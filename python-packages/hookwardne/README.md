# hookwardne

Did you mean `hookwarden`? This is a typo-defense package.

The canonical webhook security audit tool ships as the npm package `hookwarden`. Install via:

```
npx hookwarden@latest scan
```

See https://hookwarden.dev for documentation. License: Apache 2.0.

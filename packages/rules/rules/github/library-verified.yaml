schema_version: 1
rule_id: github/library-verified
provider: github
severity: info
emits_state: verified
message: |
  Library-based verification detected: `@octokit/webhooks` `verify` is
  reachable from this handler within 3 hops. The Octokit SDK enforces
  HMAC-SHA256 with constant-time comparison internally.
matcher: null
predicate: github-library-verified
applies_to:
  - express
  - hono
  - cloudflare-workers
  - vercel-edge
  - deno
  - fastify
  - nextjs
  - flask
  - fastapi
  - django
  - laravel
  - symfony
  - slim
  - vanilla-php
  # Phase 27 (RULES-GO-01): Go frameworks — an SDK-verified Go handler (github.ValidatePayload,
  # carrying sdk_verify_call evidence) emits the `verified` state instead of manual-review.
  - net-http-go
  - chi
  - gin
  - echo
provider_docs_url: https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries
references:
  - https://www.svix.com/
  - https://www.standardwebhooks.com/
fix:
  safety: manual-only
  description: "Apply the fix manually per the provider documentation — see provider_docs_url"
  codegen: null

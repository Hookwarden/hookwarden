schema_version: 1
rule_id: datadog/wrong-hmac-algorithm
provider: datadog
severity: critical
emits_state: not-verified
message: |
  Datadog webhook handler uses a manual HMAC path but the algorithm in use
  appears to be other than HMAC-SHA256. Datadog signs requests with
  HMAC-SHA256 and hex encoding — any other algorithm (MD5, SHA-1, SHA-384,
  SHA-512) will never match, so verification cannot succeed.

  Fix: `crypto.createHmac('sha256', signingSecret)` (Node),
  `hmac.new(signing_secret.encode(), raw_body, hashlib.sha256)` (Python),
  or `hash_hmac('sha256', $rawBody, $secret)` (PHP).
matcher: null
predicate: datadog-wrong-hmac-algorithm
applies_to:
  - express
  - hono
  - fastify
  - nextjs
  - flask
  - fastapi
  - django
  - laravel
  - symfony
  - slim
  - vanilla-php
provider_docs_url: https://docs.datadoghq.com/integrations/webhooks/
fix:
  safety: manual-only
  description: "Apply the fix manually per the provider documentation — see provider_docs_url"
  codegen: null

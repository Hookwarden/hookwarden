{
  "name": "@hookwarden/github-action",
  "version": "0.11.1",
  "private": true,
  "description": "GitHub Action wrapper for hookwarden CLI. Phase 5 (in-progress) — not published until real implementation lands.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "action.yml",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Hookwarden/hookwarden.git",
    "directory": "packages/github-action"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc --build",
    "bundle": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.1.1",
    "@hookwarden/pr-renderer": "workspace:*",
    "hookwarden": "workspace:*"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.0",
    "vitest": "^4.1.8"
  }
}

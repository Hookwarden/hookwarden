dist/
src/version.ts
node_modules/
# Anchored: only the package-root wasm/ (sync-wasm.mjs output) is ignored.
# The unanchored form would also swallow packages/mcp/src/wasm/loader.ts.
/wasm/
# Drift integration test stubs at tests/fixtures/drift/node_modules/** are
# force-added (git add -f) so the unanchored root `node_modules/` rule does
# not swallow them. Once tracked, gitignore stops applying. Future
# fixture-node_modules paths require the same force-add.

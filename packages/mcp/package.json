{
  "name": "@hookwarden/mcp",
  "version": "0.9.3",
  "description": "Model Context Protocol server for hookwarden — exposes the scan_handler tool so AI coding agents (Claude Code, Cursor, Continue, Anthropic Agent SDK) can find webhook signature-verification bugs locally across JavaScript, TypeScript, Python, PHP, and Go. 230 rules across 21 providers, 100% cited (CWE / RFC / Svix). Zero-network, deterministic, ships the rule pack inline with content-hash drift detection.",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "claude",
    "cursor",
    "anthropic",
    "ai-agent",
    "webhook",
    "webhook-security",
    "security",
    "audit",
    "static-analysis",
    "hookwarden"
  ],
  "homepage": "https://hookwarden.dev",
  "bugs": {
    "url": "https://github.com/Hookwarden/hookwarden/issues"
  },
  "license": "Apache-2.0",
  "type": "module",
  "mcpName": "io.github.Hookwarden/mcp",
  "bin": {
    "hookwarden-mcp": "./dist/cli.js"
  },
  "files": [
    "dist",
    "wasm",
    "README.md",
    "LICENSE",
    "server.json"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Hookwarden/hookwarden.git",
    "directory": "packages/mcp"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc -b && node scripts/emit-build-manifest.mjs",
    "inspect-bundle": "tsx scripts/inspect-bundle-mcp.ts",
    "prepare": "node scripts/sync-wasm.mjs && node scripts/sync-version.mjs",
    "prepack": "node scripts/sync-wasm.mjs && node scripts/sync-version.mjs && tsc -b && node scripts/emit-build-manifest.mjs"
  },
  "dependencies": {
    "@hookwarden/canonical-json": "workspace:*",
    "@hookwarden/engine": "workspace:*",
    "@hookwarden/rules": "workspace:*",
    "@modelcontextprotocol/sdk": "1.29.0",
    "citty": "^0.2.2",
    "picocolors": "^1.1.1",
    "web-tree-sitter": "0.26.10",
    "zod": "^3.25 || ^4.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.22.0",
    "@types/tar": "^7.0.87",
    "tar": "7.5.16",
    "tree-sitter-go": "0.25.0",
    "tree-sitter-php": "0.24.2",
    "tree-sitter-python": "^0.25.0"
  }
}
